---
description: "Implement a Rust change in CCR with a minimal diff and targeted verification"
agent: implementer
---

Implement the requested Rust change in this repository.

Checklist:
- Read `.github/copilot-instructions.md` and `.github/instructions/rust.instructions.md`.
- Touch the smallest possible set of files.
- Reuse existing abstractions and keep behavior stable unless the task explicitly changes it.
- Add or update regression tests when behavior changes.
- Run the narrowest relevant `cargo` or `just` verification and summarize the evidence.
