# 技能管理详细指南

CCR UI 提供跨平台统一技能（Skills）管理中心，支持从多种来源安装、管理和卸载技能，覆盖 Claude Code、Codex、Gemini CLI、Qwen、iFlow、Droid 等主流 AI CLI 平台。

> **版本要求**：技能管理功能自 v3.8 起引入，统一管理中心（SkillHub）和多源安装在 v4.0 中大幅增强。

## 📐 架构概览

```
┌────────────────────────────────────────────┐
│           统一技能管理中心 (SkillHub)          │
├────────────────┬───────────────────────────┤
│  已安装技能     │  技能市场 (skills.sh)       │
│  筛选 / 搜索    │  热门 / 搜索 / 批量安装      │
├────────────────┴───────────────────────────┤
│            添加技能页面 (AddSkill)            │
│  ┌──────────┐ ┌────────┐ ┌───────┐        │
│  │ GitHub   │ │  本地   │ │  npx  │        │
│  │ URL 导入  │ │ 文件夹  │ │ 安装   │        │
│  └──────────┘ └────────┘ └───────┘        │
├────────────────────────────────────────────┤
│           多平台目标选择                      │
│  Claude Code │ Codex │ Gemini │ Qwen │ ... │
└────────────────────────────────────────────┘
```

### 支持平台

| 平台 | 标识 | 技能目录 | 检测方式 |
|------|------|----------|----------|
| Claude Code | `claude-code` | `~/.claude/commands/` | 检测目录存在 |
| Codex | `codex` | `~/.codex/commands/` | 检测目录存在 |
| Gemini CLI | `gemini` | `~/.gemini/commands/` | 检测目录存在 |
| Qwen | `qwen` | `~/.qwen/commands/` | 检测目录存在 |
| iFlow | `iflow` | `~/.iflow/commands/` | 检测目录存在 |
| Droid | `droid` | `~/.droid/commands/` | 检测目录存在 |

## 🏠 技能管理主页

技能管理主页（`/skills`）采用两栏布局，提供已安装技能的全面管理能力。

### 页面布局

- **左侧筛选面板**：按平台、来源、分类、标签过滤技能
- **右侧主内容区**：
  - **统计卡片**：已安装数量、市场可用数、活跃平台数
  - **三个标签页**：已安装 / 市场 / 仓库

### 已安装标签页

展示所有已安装技能的统一视图，每个技能卡片包含：

- 技能名称和描述
- 所属平台标识
- 分类与标签
- 安装来源（marketplace / github / local）
- 版本号和作者信息
- **操作**：查看详情、编辑、删除

### 筛选功能

| 筛选维度 | 说明 | 选项 |
|----------|------|------|
| 搜索 | 按名称/描述关键词搜索 | 自由文本 |
| 来源 | 技能安装来源 | 全部 / 用户 / 插件 / 远程 |
| 分类 | 技能功能分类 | 自动从已安装技能收集 |
| 标签 | 技能标签过滤 | 多选 |
| 平台 | 目标 AI CLI 平台 | 全部 / Claude Code / Codex / ... |

### 市场标签页

浏览 [skills.sh](https://skills.sh) 技能市场：

- **热门浏览**：按星标数排列的热门技能
- **关键词搜索**：在市场中搜索技能
- **一键安装**：选择目标平台后直接安装
- **批量安装**：批量选择后一次安装多个技能

### 技能详情

点击技能卡片可查看详情，包括：

- **SKILL.md 内容**：完整的技能说明（Markdown 渲染）
- **Frontmatter 元数据**：作者、版本、许可证、分类、标签
- **编辑模式**：直接编辑 SKILL.md 内容
- **安装元信息**：安装来源、安装时间、commit hash

## ➕ 添加技能页面

添加技能页面（`/skills/add`）提供两种安装方式：**市场浏览** 和 **手动安装**。

### 市场浏览安装

#### 搜索与排序

- **搜索框**：实时搜索市场技能（300ms 防抖）
- **排序**：按星标数 / 按名称
- **缓存状态**：显示市场数据是否来自缓存
- **刷新缓存**：手动更新市场数据

#### 技能卡片

每个市场技能卡片显示：

- 所有者头像和名称
- 技能名称和描述
- GitHub 星标数
- skills.sh 链接
- **安装按钮**：点击后弹出平台选择模态框

#### 批量安装

1. 点击「批量模式」进入多选状态
2. 勾选需要安装的技能
3. 底部浮动操作栏显示已选数量
4. 点击「批量安装」，选择目标平台后一键安装

#### 安装流程

```
点击安装 → 选择目标平台 → 下载技能包 → 复制到平台目录 → 完成
                              ↓
                    Toast 实时显示安装进度
```

### 手动安装

手动安装支持三种来源，通过标签页切换：

#### GitHub URL 导入

从 GitHub 仓库直接导入技能。

**支持格式**：
- `https://github.com/owner/repo` — 整个仓库
- `https://github.com/owner/repo/tree/branch/path` — 指定路径
- `owner/repo` — 简写格式

**流程**：
1. 输入 GitHub URL
2. 选择目标平台
3. 后端 clone 仓库并提取技能文件
4. 复制到目标平台的技能目录

#### 本地文件夹导入

从本地文件系统导入技能。

**操作**：
1. 输入本地路径或点击「浏览」按钮选择文件夹
2. 选择目标平台
3. 后端复制技能文件到目标平台目录

> 💡 **提示**：本地导入适合开发中的自定义技能，可以快速测试。

#### npx 安装

通过 `npx` 执行 npm 包来安装技能。

**前置条件**：系统需安装 Node.js 和 npm。

**状态检测**：页面加载时自动检测 npx 可用性，显示版本号。

**选项**：
- **包名**：npm 包名（如 `@anthropic/skill-code-review`）
- **全局安装**：勾选后使用 `--global` 标志

> ⚠️ **注意**：如果 npx 不可用，系统会自动回退到 GitHub 方式安装。

### 目标平台选择

所有安装方式共享统一的目标平台选择器：

- 显示所有支持的平台，标记已检测和未检测的
- **快捷操作**：「选择已检测」一键勾选所有已安装的平台
- **清除所有**：一键取消所有选择
- 未检测的平台可以勾选但标记为灰色

### 安装进度提示

安装过程通过 Toast 组件实时反馈：

| 阶段 | 状态 | 说明 |
|------|------|------|
| `idle` | 空闲 | 无操作 |
| `downloading` | 下载中 | 正在从远程获取技能包 |
| `installing` | 安装中 | 正在复制文件到目标目录 |
| `done` | 完成 | 安装成功 |
| `error` | 错误 | 安装失败，显示错误信息 |

## 🔌 后端 API

### SkillHub API

统一技能中心的核心接口。

#### 获取平台代理列表

```http
GET /api/skill_hub/agents
```

返回所有支持平台的状态：

```json
[
  {
    "id": "claude-code",
    "display_name": "Claude Code",
    "global_skills_dir": "~/.claude/commands",
    "detected": true,
    "installed_count": 12
  }
]
```

#### 获取平台已安装技能

```http
GET /api/skill_hub/agents/{agent}/skills
```

返回指定平台已安装的技能列表。

#### 市场热门技能

```http
GET /api/skill_hub/marketplace/trending?limit=50&page=1
```

返回 skills.sh 热门技能，支持分页。

#### 市场搜索

```http
GET /api/skill_hub/marketplace/search?q=code-review&limit=20&page=1
```

按关键词搜索市场技能。

#### 安装技能

```http
POST /api/skill_hub/install
Content-Type: application/json

{
  "package": "owner/repo",
  "agents": ["claude-code", "codex"],
  "force": false
}
```

安装指定技能到目标平台。

#### 卸载技能

```http
POST /api/skill_hub/remove
Content-Type: application/json

{
  "skill": "skill-name",
  "agents": ["claude-code"]
}
```

### 多源导入 API

#### GitHub URL 导入

```http
POST /api/skill_hub/import/github
Content-Type: application/json

{
  "url": "https://github.com/owner/repo",
  "agents": ["claude-code", "gemini"],
  "force": false
}
```

#### 本地文件夹导入

```http
POST /api/skill_hub/import/local
Content-Type: application/json

{
  "sourcePath": "/path/to/skill",
  "agents": ["claude-code"],
  "skillName": "my-skill"
}
```

#### npx 安装

```http
POST /api/skill_hub/import/npx
Content-Type: application/json

{
  "package": "@scope/skill-package",
  "agents": ["claude-code"],
  "global": false
}
```

#### 批量安装

```http
POST /api/skill_hub/install/batch
Content-Type: application/json

{
  "packages": ["owner1/repo1", "owner2/repo2"],
  "agents": ["claude-code", "codex"],
  "force": false
}
```

**响应**：

```json
{
  "total": 3,
  "successCount": 2,
  "failCount": 1,
  "results": [
    { "package": "owner1/repo1", "ok": true },
    { "package": "owner2/repo2", "ok": true },
    { "package": "owner3/repo3", "ok": false, "message": "Repository not found" }
  ]
}
```

#### npx 可用性检测

```http
GET /api/skill_hub/npx/status
```

```json
{
  "available": true,
  "version": "10.2.3",
  "path": "/usr/local/bin/npx"
}
```

#### 浏览文件夹（Tauri 模式）

```http
POST /api/skill_hub/browse
```

打开系统文件夹选择对话框，返回用户选择的路径。

#### 刷新市场缓存

```http
POST /api/skill_hub/marketplace/refresh
```

强制刷新 skills.sh 市场数据缓存。

### 传统 Skills API

基础技能管理接口（兼容旧版本）。

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/skills` | 获取技能列表 |
| `POST` | `/api/skills` | 创建技能 |
| `DELETE` | `/api/skills/{name}` | 删除技能 |
| `GET` | `/api/skills/repositories` | 获取技能仓库列表 |
| `POST` | `/api/skills/repositories` | 添加技能仓库 |
| `DELETE` | `/api/skills/repositories/{name}` | 移除技能仓库 |
| `GET` | `/api/skills/repositories/{name}/scan` | 扫描仓库技能 |

## 🎨 前端组件

技能管理涉及的前端组件：

### 页面组件

| 组件 | 路径 | 说明 |
|------|------|------|
| `UnifiedSkillsView` | `views/skills/UnifiedSkillsView.vue` | 技能管理主页 |
| `AddSkillView` | `views/skills/AddSkillView.vue` | 添加技能页面 |

### 业务组件

| 组件 | 说明 |
|------|------|
| `SkillsFilterPanel` | 左侧筛选面板（平台、来源、分类、标签） |
| `SkillsStatsCards` | 统计卡片（已安装、可用、活跃平台） |
| `SkillsInstalledTab` | 已安装技能列表标签页 |
| `SkillsMarketplaceTab` | 市场浏览标签页（懒加载） |
| `MarketplaceSkillCard` | 市场技能卡片 |
| `MarketplacePagination` | 市场分页组件 |
| `SkillInstallModal` | 安装确认模态框（选择目标平台） |
| `SkillDetailModal` | 技能详情/编辑模态框（懒加载） |
| `SkillDeleteConfirmModal` | 删除确认模态框（懒加载） |
| `SkillOperationLogModal` | 操作日志模态框（懒加载） |
| `SkillInstallToast` | 安装进度 Toast 提示 |

### Composable

| 名称 | 说明 |
|------|------|
| `useUnifiedSkills` | 统一技能管理状态和方法 |

## 📂 数据模型

### UnifiedSkill（统一技能）

```typescript
interface UnifiedSkill {
  name: string           // 技能名称
  description?: string   // 技能描述
  skillDir: string       // 技能目录路径
  platform: Platform     // 所属平台
  platformName: string   // 平台显示名
  category?: string      // 分类
  tags: string[]         // 标签列表
  version?: string       // 版本号
  author?: string        // 作者
  source?: string        // 安装来源: "marketplace" | "github" | "local"
  sourceUrl?: string     // 来源 URL
  installDate?: number   // 安装时间戳 (ms)
  commitHash?: string    // Git commit hash
}
```

### MarketplaceItem（市场技能项）

```typescript
interface MarketplaceItem {
  package: string        // 包标识（owner/repo 格式）
  owner: string          // 仓库所有者
  repo: string           // 仓库名
  skill?: string         // 技能名（可能与 repo 不同）
  skillsShUrl: string    // skills.sh 链接
  description?: string   // 技能描述
  authorAvatar?: string  // 作者头像 URL
  stars?: number         // GitHub 星标数
}
```

## 🔄 常见使用场景

### 场景 1：从市场快速安装

1. 进入技能管理页面 → 点击「添加技能」
2. 在热门列表或搜索中找到目标技能
3. 点击「安装」→ 选择目标平台（默认勾选已检测平台）
4. 等待安装完成，Toast 提示结果

### 场景 2：批量安装热门技能

1. 在添加技能页面点击「批量模式」
2. 勾选多个想安装的技能
3. 点击底部浮动操作栏的「批量安装」
4. 选择目标平台后一键安装

### 场景 3：从 GitHub 安装自定义技能

1. 进入添加技能页面 → 切换到「手动安装」
2. 选择 GitHub 标签页
3. 输入仓库 URL（如 `https://github.com/user/my-skill`）
4. 选择目标平台 → 安装

### 场景 4：导入本地开发中的技能

1. 在本地开发好技能文件
2. 进入添加技能页面 → 手动安装 → 本地标签页
3. 输入路径或点击「浏览」选择文件夹
4. 选择目标平台 → 安装

### 场景 5：管理已安装技能

1. 在技能管理主页查看所有已安装技能
2. 使用筛选面板按平台、分类过滤
3. 点击技能卡片查看详情或编辑
4. 删除不需要的技能

## ❓ 故障排查

### 常见问题

**Q: 市场数据加载缓慢？**
- 市场数据通过 skills.sh 获取，首次请求可能较慢
- 后端会缓存市场数据，后续请求更快
- 点击「刷新缓存」可强制更新

**Q: GitHub 导入失败？**
- 检查 URL 格式是否正确
- 确认仓库为公开仓库或本地已配置 Git 认证
- 检查网络连接和代理设置

**Q: npx 安装显示不可用？**
- 确保已安装 Node.js（建议 v18+）
- 检查 `npx` 命令是否在 PATH 中
- 尝试运行 `npx --version` 验证

**Q: 安装到未检测的平台失败？**
- 确认该平台的 CLI 工具已安装
- 检查技能目录是否存在且有写入权限
- 未检测的平台仍可尝试安装，但目录需要手动创建

**Q: 技能安装后不生效？**
- 部分平台可能需要重启 CLI 工具
- 检查技能目录中是否存在有效的 SKILL.md 文件
- 确认技能文件格式与目标平台兼容

更多问题请参阅 [常见问题](../faq.md) 页面。
