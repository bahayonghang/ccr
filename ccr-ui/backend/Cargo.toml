[package]
name = "ccr-ui-backend"
version = "3.6.0"
edition = "2024"

[dependencies]
# 🎯 CCR 核心库 - 直接使用服务层（无子进程开销）
ccr = { path = "../../", default-features = false }

# ═══════════════════════════════════════════════════════════
# 📦 Workspace 依赖（统一版本管理）
# ═══════════════════════════════════════════════════════════

# Web framework (Axum + Tower ecosystem)
axum = { workspace = true, features = ["macros"] }
tower = { workspace = true, features = ["timeout", "limit"] }
tower-http = { workspace = true, features = ["fs", "trace", "cors", "compression-full", "request-id", "util"] }

# Async runtime
tokio = { workspace = true, features = ["full"] }

# Serialization (从 workspace 继承)
serde = { workspace = true }
serde_json = { workspace = true }

# Time (从 workspace 继承)
chrono = { workspace = true }

# Error handling (从 workspace 继承)
thiserror = { workspace = true }

# Config file parsing (从 workspace 继承)
toml = { workspace = true }
dirs = { workspace = true }

# ═══════════════════════════════════════════════════════════
# 📦 Backend 专有依赖
# ═══════════════════════════════════════════════════════════

# Async helpers
futures = "0.3"
async-stream = "0.3"

# Parallel processing
rayon = "1.10"
lazy_static = "1.5"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json", "fmt", "ansi"] }
tracing-appender = "0.2"

# CLI arguments
clap = { version = "4.5.53", features = ["derive"] }

# System information
whoami = "1.5"
num_cpus = "1.17.0"
sysinfo = "0.37.2"  # 统一使用与 ccr 相同的版本

# HTTP client for checking updates
reqwest = { workspace = true }

# YAML support (for special cases)
serde_yaml = "0.9.33"

# Atomic file operations (从 workspace 继承)
tempfile = { workspace = true }

# File system utilities
walkdir = "2.5"

# ✅ 依赖优化完成说明：
# - toml, dirs, tempfile, thiserror 已改用 workspace 依赖
# - serde, serde_json, chrono, reqwest 已改用 workspace 依赖
# - 版本统一由根 Cargo.toml 的 workspace.dependencies 管理
# - 减少了版本冲突风险，加快编译速度

