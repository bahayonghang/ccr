[package]
name = "ccr-ui-backend"
version = "2.5.0"
edition = "2024"

[dependencies]
# 🎯 CCR 核心库 - 直接使用服务层（无子进程开销）
ccr = { path = "../../", default-features = false }

# Web framework (Axum + Tower ecosystem)
axum = { version = "0.7", features = ["macros"] }
tower = { version = "0.5", features = ["timeout", "limit"] }
tower-http = { version = "0.6", features = ["fs", "trace", "cors", "compression-full", "request-id", "util"] }

# Async runtime
tokio = { version = "1.48.0", features = ["full"] }
futures = "0.3"
async-stream = "0.3"

# Serialization (ccr 已提供 serde, serde_json, 保留以确保兼容性)
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Time (ccr 已提供 chrono)
chrono = { version = "0.4", features = ["serde"] }

# Error handling (暂时保留，重构后可移除)
thiserror = "2.0"

# Config file parsing (暂时保留，重构后可移除)
toml = "0.9"
dirs = "6.0"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json", "fmt", "ansi"] }
tracing-appender = "0.2"

# CLI arguments
clap = { version = "4.5", features = ["derive"] }

# System information
whoami = "1.5"
num_cpus = "1.16"
sysinfo = "0.32"

# HTTP client for checking updates
reqwest = { version = "0.12", features = ["json"] }

# YAML support (for special cases)
serde_yaml = "0.9"

# Atomic file operations (暂时保留，重构后可移除)
tempfile = "3.10"

# 🗑️ 将在重构后移除的依赖（ccr 已提供）:
# - toml = "0.9"        ✓ ccr 已提供，但旧代码仍在使用
# - dirs = "6.0"        ✓ ccr 已提供，但旧代码仍在使用
# - tempfile = "3.10"   ✓ ccr 已提供，但旧代码仍在使用
# - thiserror = "2.0"   ✓ ccr 已提供，但旧代码仍在使用
# - anyhow = "1.0"      ✓ ccr 已提供（这个已经成功移除）

