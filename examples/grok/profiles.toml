# CCR Grok profiles
# Initialize this file with: ccr grok profile init
# Target path: ~/.ccr/platforms/grok/profiles.toml

default_config = "official"
current_config = ""

[settings]
skip_confirmation = false

# Official Grok uses Grok's own session authentication. XAI_API_KEY may also
# be configured globally for Grok; CCR does not copy that credential here.
[official]
description = "Grok official model"
model = "grok-example"
provider = "xai"
enabled = true

# Third-party relay: CCR stores only the environment variable name.
[relay]
description = "Example Grok-compatible relay"
base_url = "https://api.example.com/v1"
model = "grok-example"
provider = "example-relay"
provider_type = "third_party_model"
api_backend = "responses"
env_key = "GROK_RELAY_API_KEY"
context_window = 1000000
supports_backend_search = true
enabled = true

# Inline credentials are supported but intentionally omitted from copy-ready
# examples. To create one explicitly, use:
# ccr grok profile create <name> --base-url <url> --model <model> --auth-token <token>
