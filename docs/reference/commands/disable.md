# disable - 禁用配置

禁用指定的配置方案，使其暂时不可切换使用，但保留配置信息。

## 用法

```bash
ccr disable <config_name> [OPTIONS]
```

## 参数

- `<config_name>`: 要禁用的配置名称（必需）

## 选项

- `-f, --force`: 强制禁用（即使是当前正在使用的配置）

## 功能特性

- 🚫 **临时禁用** - 禁用配置但保留配置信息
- 🔒 **安全检查** - 检测当前配置，防止误操作
- 💾 **可恢复** - 可以随时通过 `enable` 命令重新启用
- 📝 **状态标记** - 在配置列表中清晰显示禁用状态
- 📋 **历史记录** - 记录禁用操作到审计日志

## 示例

### 基本用法

```bash
# 禁用指定配置
ccr disable test_config

# 强制禁用（即使是当前配置）
ccr disable current_config --force

# 使用短选项
ccr disable current_config -f
```

## 示例输出

### 成功禁用

```bash
$ ccr disable test_provider

禁用配置: test_provider
══════════════════════════════════════════════════════════════

▶ 步骤 1/3: 检查配置
✓ 配置 'test_provider' 存在

▶ 步骤 2/3: 安全检查
✓ 安全检查完成

▶ 步骤 3/3: 更新状态
✓ 配置 'test_provider' 已禁用

ℹ 后续操作:
  • 运行 'ccr list' 查看所有配置
  • 使用 'ccr enable test_provider' 重新启用
```

### 禁用当前配置（警告）

```bash
$ ccr disable anyrouter

禁用配置: anyrouter
══════════════════════════════════════════════════════════════

▶ 步骤 1/3: 检查配置
✓ 配置 'anyrouter' 存在

▶ 步骤 2/3: 安全检查
⚠ 配置 'anyrouter' 是当前激活的配置

ℹ 禁用当前配置后:
  1. 当前 settings.json 仍保持现有设置
  2. 之后无法再切换回此配置（除非重新启用）
  3. 建议先切换到其他配置

✗ 无法禁用当前配置（使用 --force 强制禁用）
```

### 强制禁用当前配置

```bash
$ ccr disable anyrouter --force

禁用配置: anyrouter
══════════════════════════════════════════════════════════════

▶ 步骤 1/3: 检查配置
✓ 配置 'anyrouter' 存在

▶ 步骤 2/3: 安全检查
⚠ 配置 'anyrouter' 是当前激活的配置
ℹ 使用 --force 模式，强制禁用

▶ 步骤 3/3: 更新状态
✓ 配置 'anyrouter' 已强制禁用

⚠ 重要提示:
  • 当前 settings.json 仍使用此配置的设置
  • 之后无法再切换回此配置（除非重新启用）
  • 建议尽快切换到其他配置:
    1. ccr list        # 查看可用配置
    2. ccr switch <name>  # 切换到其他配置
```

### 配置已禁用

```bash
$ ccr disable test_config

禁用配置: test_config
══════════════════════════════════════════════════════════════

▶ 步骤 1/3: 检查配置
✓ 配置 'test_config' 存在

▶ 步骤 2/3: 安全检查
ℹ 配置 'test_config' 已经是禁用状态

✓ 无需更改
```

### 配置不存在

```bash
$ ccr disable nonexistent

禁用配置: nonexistent
══════════════════════════────────────────────────────────────

▶ 步骤 1/3: 检查配置
✗ 配置节 'nonexistent' 不存在

建议: 运行 'ccr list' 查看可用配置
```

## 使用场景

### 场景 1: 临时停用配置

```bash
# 临时停用某个配置
ccr disable test_config

# 需要时重新启用
ccr enable test_config
```

### 场景 2: 清理过期配置（保留数据）

```bash
# 查看所有配置
ccr list

# 禁用过期配置（保留配置信息）
ccr disable old_api_2023

# 如果确定不需要，再删除
ccr delete old_api_2023
```

### 场景 3: 配置分类管理

```bash
# 只保留生产环境配置为启用状态
ccr disable test_config1
ccr disable test_config2
ccr disable dev_config

# 生产配置保持启用
# prod_config  [已启用]
```

### 场景 4: 防止误切换

```bash
# 禁用危险配置（防止误切换）
ccr disable production_critical

# 需要使用时再启用
ccr enable production_critical
ccr switch production_critical
```

## 禁用 vs 删除 vs 删除状态

| 操作 | 保留配置 | 可切换 | 可恢复 | 适用场景 |
|------|---------|-------|--------|----------|
| **禁用** | ✅ 保留 | ❌ 不可切换 | ✅ 立即可用 | • 临时不用<br>• 防止误操作<br>• 配置分类管理 |
| **删除** | ❌ 移除 | ❌ 不可切换 | ⚠️ 需要导入或重新添加 | • 永久移除<br>• 清理无用配置<br>• 释放配置空间 |
| **当前** | ✅ 保留 | ✅ 正在使用 | - | • 当前激活的配置 |

## 安全检查机制

### 检查 1: 配置存在性

验证要禁用的配置是否存在于配置文件中。

```bash
# 配置存在 → 继续
✓ 配置 'test' 存在

# 配置不存在 → 终止
✗ 配置节 'nonexistent' 不存在
```

### 检查 2: 当前配置保护

默认情况下，不允许禁用当前激活的配置：

```bash
⚠ 配置 'xxx' 是当前激活的配置

ℹ 禁用当前配置后:
  1. 当前 settings.json 仍保持现有设置
  2. 之后无法再切换回此配置（除非重新启用）

✗ 无法禁用当前配置（使用 --force 强制禁用）
```

**使用 --force 可以强制禁用：**

```bash
ccr disable current_config --force
```

## 配置状态

### 禁用前

```toml
[test_config]
enabled = true   # 或无此字段（默认为 true）
base_url = "https://api.test.com"
auth_token = "test-token"
```

### 禁用后

```toml
[test_config]
enabled = false  # 设置为 false
base_url = "https://api.test.com"
auth_token = "test-token"  # 配置信息保留
```

### 重新启用

```bash
ccr enable test_config
```

```toml
[test_config]
enabled = true   # 恢复为 true
base_url = "https://api.test.com"
auth_token = "test-token"
```

## 在列表中的显示

```bash
$ ccr list

配置列表
══════════════════════════════════════════════════════════════

anthropic    [已启用] [当前]  Anthropic Official API
test_config  [已禁用]         Test Configuration
prod_config  [已启用]         Production Configuration
```

被禁用的配置会标记为 `[已禁用]`，且无法通过 `ccr switch` 切换。

## 恢复禁用的配置

### 方法 1: 使用 enable 命令（推荐）

```bash
ccr enable test_config
```

### 方法 2: 手动编辑配置文件

编辑 `~/.ccs_config.toml`（Legacy 模式）或 `~/.ccr/platforms/<platform>/profiles.toml`（Unified 模式）：

```toml
[test_config]
enabled = true  # 改为 true
...
```

## 常见问题

### Q: 禁用当前配置后 Claude Code 会停止工作吗？

**A:** 不会，禁用操作不会修改 `settings.json`：
- Claude Code 会继续使用当前设置
- 但之后无法再切换回此配置（除非重新启用）

### Q: 可以恢复禁用的配置吗？

**A:** 可以，使用 `enable` 命令即可：
```bash
ccr enable test_config
```

### Q: 禁用和删除有什么区别？

**A:**
- **禁用**：保留配置信息，可随时启用
- **删除**：完全移除配置，需要导入或重新添加才能恢复

### Q: 禁用配置会影响配置文件吗？

**A:** 会修改配置的 `enabled` 字段为 `false`，但不会删除配置信息。

### Q: 强制禁用当前配置有什么风险？

**A:** 主要风险：
- 之后无法切换回此配置（除非重新启用）
- 需要手动切换到其他配置

建议：
```bash
# 更安全的方式：先切换再禁用
ccr switch other_config
ccr disable old_config
```

## 最佳实践

### 1. 先切换再禁用

```bash
# 推荐：先切换到其他配置
ccr switch new_config
ccr disable old_config

# 不推荐：强制禁用当前配置
ccr disable current_config --force
```

### 2. 使用禁用功能进行配置分类

```bash
# 生产环境配置 - 保持启用
# prod_anthropic  [已启用]
# prod_openai     [已启用]

# 测试环境配置 - 禁用
ccr disable test_config1
ccr disable test_config2

# 个人配置 - 根据需要启用/禁用
ccr disable personal_backup
```

### 3. 防止误操作

```bash
# 禁用危险配置
ccr disable critical_production

# 需要时再启用
ccr enable critical_production
ccr switch critical_production
```

### 4. 临时不用的配置

```bash
# 临时不用，但可能还会用
ccr disable temp_config

# 确定不用，删除
ccr delete useless_config
```

## 注意事项

::: danger 危险操作
- 使用 `--force` 强制禁用当前配置可能导致无法切换回此配置
- 建议先切换到其他配置再禁用
:::

::: warning 注意
- 禁用配置不会修改 `settings.json`
- 禁用后无法通过 `ccr switch` 切换到该配置
- 禁用的配置仍占用配置文件空间
:::

::: tip 建议
- 临时不用的配置使用 `disable`
- 永久不用的配置使用 `delete`
- 禁用当前配置前先切换到其他配置
- 定期检查并清理禁用的配置
:::

## 相关命令

- [enable](./enable) - 启用配置
- [list](./list) - 查看所有配置及状态
- [switch](./switch) - 切换到指定配置
- [delete](./delete) - 删除配置
- [add](./add) - 添加新配置
