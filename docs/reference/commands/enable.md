# enable - 启用配置

启用被禁用的配置方案，使其可以正常切换使用。

## 用法

```bash
ccr enable <config_name>
```

## 参数

- `<config_name>`: 要启用的配置名称（必需）

## 功能特性

- ✅ **状态管理** - 修改配置的启用/禁用状态
- 🔍 **配置验证** - 启用前检查配置是否存在
- 📝 **状态显示** - 显示配置的当前状态
- 💾 **持久保存** - 更新配置文件中的状态标记
- 📋 **历史记录** - 记录启用操作到审计日志

## 示例

### 基本用法

```bash
# 启用被禁用的配置
ccr enable test_config

# 启用多个配置
ccr enable config1
ccr enable config2
```

## 示例输出

### 成功启用

```bash
$ ccr enable test_provider

启用配置: test_provider
══════════════════════════════════════════════════════════════

▶ 步骤 1/2: 检查配置
✓ 配置 'test_provider' 存在

▶ 步骤 2/2: 更新状态
✓ 配置 'test_provider' 已启用

ℹ 后续操作:
  • 运行 'ccr list' 查看所有配置
  • 运行 'ccr switch test_provider' 切换到此配置
```

### 配置已启用

```bash
$ ccr enable anthropic

启用配置: anthropic
══════════════════════════════════════════════════════════════

▶ 步骤 1/2: 检查配置
✓ 配置 'anthropic' 存在

▶ 步骤 2/2: 更新状态
ℹ 配置 'anthropic' 已经是启用状态

✓ 无需更改
```

### 配置不存在

```bash
$ ccr enable nonexistent

启用配置: nonexistent
══════════════════════════════════════════════════════════════

▶ 步骤 1/2: 检查配置
✗ 配置节 'nonexistent' 不存在

建议: 运行 'ccr list' 查看可用配置
```

## 使用场景

### 场景 1: 启用临时禁用的配置

```bash
# 临时禁用某个配置（测试期间）
ccr disable test_config

# 测试完成后重新启用
ccr enable test_config

# 切换使用
ccr switch test_config
```

### 场景 2: 批量管理配置状态

```bash
# 查看所有配置
ccr list

# 启用需要的配置
ccr enable prod_config
ccr enable backup_config

# 禁用不需要的配置
ccr disable old_config
```

### 场景 3: 切换工作环境

```bash
# 工作环境配置
ccr enable work_config
ccr switch work_config

# 下班后切换到个人配置
ccr enable personal_config
ccr switch personal_config
ccr disable work_config
```

## 配置状态

### 启用状态（Enabled）

- ✅ 可以通过 `ccr switch` 切换使用
- ✅ 在 `ccr list` 中正常显示
- ✅ 可以被设为当前配置
- ✅ 所有 CCR 命令都可以操作

### 禁用状态（Disabled）

- ❌ 不能通过 `ccr switch` 切换使用
- ⚠️ 在 `ccr list` 中标记为禁用
- ❌ 不能被设为当前配置
- ✅ 仍然保留在配置文件中

## 启用 vs 删除

| 操作 | 保留配置 | 可恢复 | 使用场景 |
|------|---------|--------|----------|
| **禁用** | ✅ 保留 | ✅ 立即可用 | • 临时不用<br>• 保留配置信息 |
| **删除** | ❌ 移除 | ⚠️ 需要恢复 | • 永久移除<br>• 清理无用配置 |

## 与 list 命令配合

```bash
# 查看所有配置及其状态
ccr list

# 输出示例：
# anthropic    [已启用] [当前]
# test_config  [已禁用]
# prod_config  [已启用]

# 启用被禁用的配置
ccr enable test_config

# 再次查看
ccr list

# 输出示例：
# anthropic    [已启用] [当前]
# test_config  [已启用]  ← 状态已改变
# prod_config  [已启用]
```

## 常见问题

### Q: 启用配置后会自动切换吗？

**A:** 不会，启用只是改变配置的状态标记。需要手动运行：
```bash
ccr enable test_config
ccr switch test_config  # 手动切换
```

### Q: 可以启用当前配置吗？

**A:** 可以，但通常当前配置已经是启用状态：
```bash
ccr enable current_config  # 如果已启用，会提示无需更改
```

### Q: 启用操作会修改配置内容吗？

**A:** 不会，只修改配置的启用/禁用状态标记，不影响配置的内容（API Key、Base URL 等）。

### Q: 启用和 switch 有什么区别？

**A:**
- `enable`：改变配置的**状态标记**（启用/禁用）
- `switch`：改变**当前使用的配置**（需要配置是启用状态）

```bash
# 完整流程
ccr enable test_config   # 1. 启用配置
ccr switch test_config   # 2. 切换到该配置
```

## 最佳实践

### 1. 配置分类管理

```bash
# 生产环境配置 - 保持启用
ccr enable prod_anthropic
ccr enable prod_openai

# 测试环境配置 - 需要时启用
ccr disable test_config1
ccr disable test_config2

# 个人配置 - 根据需要启用/禁用
ccr enable personal_config
```

### 2. 定期检查配置状态

```bash
# 查看所有配置
ccr list

# 清理不需要的配置
ccr disable old_config
# 或永久删除
ccr delete old_config
```

### 3. 使用描述性标记

虽然 CCR 没有内置标签功能，但可以通过命名区分：

```bash
# 使用前缀标识类型
# prod_*    - 生产环境
# test_*    - 测试环境
# backup_*  - 备份配置

ccr enable prod_main
ccr disable test_temp
```

### 4. 配合自动化脚本

```bash
#!/bin/bash
# 切换到工作环境

# 禁用个人配置
ccr disable personal_config

# 启用工作配置
ccr enable work_config
ccr switch work_config

echo "✓ 已切换到工作环境"
```

## 注意事项

::: tip 提示
- 启用操作是**幂等的**，多次启用同一配置不会出错
- 启用后配置仍需要 `switch` 才能使用
- 启用操作不会验证配置的有效性（API Key 等）
:::

::: warning 注意
- 启用操作会修改配置文件
- 如果配置已启用，操作会提示但不会报错
- 启用不等于切换，仍需 `ccr switch` 才能使用
:::

## 相关命令

- [disable](./disable) - 禁用配置
- [list](./list) - 查看所有配置及状态
- [switch](./switch) - 切换到指定配置
- [add](./add) - 添加新配置
- [delete](./delete) - 删除配置
