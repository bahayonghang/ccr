# nextTick 登记表（批次 1 排查 → 批次 5 填写替代实现）

> R3 / AC4。全仓 52 处 `nextTick` 中落在本任务范围（`src/stores` + `src/composables`）的调用点排查。

## 排查结果（2026-08-23，`rg -n nextTick src/stores src/composables`）

**0 处**。全部 52 处落在视图层（`.vue`），由各视图子任务（6–12）按其对应 AC 登记。

## 表（调用点 → 替代实现）

| 文件 | 行 | 原始意图 | 替代实现 |
| --- | --- | --- | --- |
| （本任务范围内无调用点） | — | — | — |
