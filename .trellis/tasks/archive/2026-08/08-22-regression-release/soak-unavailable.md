# 2 小时长时间运行：未执行（AC13）

> 任务：`08-22-regression-release`。对应 R10 / AC13 / implement.md 步骤 6。

## 结论

未执行。不标为通过。

## 原因

本会话时间盒用于 185 行逐屏归属记录与可证明的 smoke / 配置核对。2 小时连续运行、20+ 界面切换、内存后半段均值 ≤ 前半段 110%、事件监听器计数，均超出该时间盒。

主线程指令禁止本会话执行 `just tauri-build`。design.md §9 要求长时间运行在无回归缺陷的**打包产物**上做。当前 `just frontend-check` 在 `frontend-build` 失败（见 `defects.md` D1），不具备该产物。

## 未采集的数据

| 项 | 状态 |
| --- | --- |
| 连续运行 2 小时 | 未执行 |
| 切换 ≥20 个界面 | 未执行 |
| 内存采样间隔 | 未定（design.md §11 未决） |
| 第 2 小时均值 / 第 1 小时均值 | 无数据 |
| 事件监听器数量 | 未执行（design.md §5 计数方式未定） |

相关但范围不同的已有证据：`event-bridge-leak.smoke.test.tsx` 覆盖桥接层泄漏；`08-22-views-sync-tools` AC7 为 30 分钟日志流，不是本项 2 小时全局浸泡。
